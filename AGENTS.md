# AGENTS.md
<!-- Generated by APM CLI from distributed .apm/ primitives -->
<!-- Build ID: fa954a46a204 -->

## Global Instructions

## Style

- Short, direct, technical prose
- On feedback/analysis: say agree or disagree, then what changed

## Code quality

- Read files in full before broad edits, audits, or changing unread files; do not rely on search snippets
- Ask before removing intentional behavior
- One clear approach; refactor duplication; no weird parallel defaults
- Propagate failures (throw/exit); no silent fallbacks; no shims unless asked
- Prefer a small function with a long, descriptive name over a comment that restates what the code does; reserve comments for non-obvious details (formats, limits, platform quirks)
- Match the language and conventions of the project you are in; do not impose another project's stack

## Monorepos

When working in a monorepo:

- Treat each top-level project as independent unless it explicitly opts into shared tooling or dependencies
- Prefer project-local configuration and follow project-local `AGENTS.md` and `.agents/skills/`
- Do not invent a root workspace or toolchain
- Never edit or delete the root `README.md`

## Commands

- Use documented tasks/scripts (README, `package.json` scripts, `deno task`, `make`, etc.)
- After creating/modifying a test: run it until it passes. No sleeps in tests.

## Skills

Repo-wide skills are an [APM](https://microsoft.github.io/apm/) package (`apm.yml`): source `.apm/skills/<name>/SKILL.md`, installed `.agents/skills/<name>/SKILL.md` (what agents read). Always-on rules are `.apm/instructions/*.instructions.md`; after editing them, run `apm install`, then `apm compile --target cursor --local-only --no-links --no-constitution` to regenerate Cursor rules and `AGENTS.md`. Cursor hooks are native (`.cursor/hooks.json`, `.cursor/hooks/`); APM does not manage them. In a monorepo, also `<project>/.agents/skills/` when present. When a task matches a skill description, read and follow the installed `SKILL.md`. **Before any commit**, read `.agents/skills/git-commits/SKILL.md` even if the user did not mention commits. After editing a skill, run `apm install`. Other repos: `weakish/vibe` (skills and instructions).

| Skill | Path |
|-------|------|
| Git commits and trailers | `.agents/skills/git-commits/SKILL.md` |
| Review one commit; fix after selection | `.agents/skills/review/SKILL.md` |
| Herdr (only when the user asks) | `.agents/skills/herdr/SKILL.md` |

## Herdr

Herdr is a terminal multiplexer for coding agents (workspaces, tabs, panes). Use it **only** when the user mentions Herdr or asks to inspect/control panes, tabs, workspaces, commands, or another agent — not merely because work could run in a background pane. Requires `HERDR_ENV=1`; if unset or not `1`, say you are not inside Herdr and stop. When using it, read and follow `.agents/skills/herdr/SKILL.md`.

## Git (multi-agent cwd)

Other agents may be editing different files here. Never touch their unstaged/staged/untracked files.

- Stage/commit only files **you** changed in **this** session; verify with `git status` first
- If you added, edited, or deleted any non-gitignored file this session, **commit before finishing your turn** — read the git-commits skill, stage your files, and commit. Never ask the user whether to commit
- Never push, force-push, or push/merge to `master` (even if asked)
- Never: `git reset --hard`, `git checkout .`, `git clean -fd`, `git stash`, `git add -A`, `git add .`, `git commit --no-verify`
- Rebase conflicts: resolve only your files; otherwise abort and ask
- Issues/PRs: only when explicitly asked; PRs must be drafts
- Bug fixes for user-reported issues: `Reported-by` on the fix commit (see git-commits skill)
- Changes shaped by a human’s idea or approach: `Suggested-by` on that commit (see git-commits skill)
